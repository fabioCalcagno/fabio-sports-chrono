import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'br.com.fabiocalcagnodev',
  appName: 'Sports Chrono',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
