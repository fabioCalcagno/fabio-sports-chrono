import { ObjetoParaArrayPipe } from './object-to-array.pipe';

describe('ObjetoParaArrayPipe', () => {
  it('create an instance', () => {
    const pipe = new ObjetoParaArrayPipe();
    expect(pipe).toBeTruthy();
  });
});
